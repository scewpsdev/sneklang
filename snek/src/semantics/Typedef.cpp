#include "Typedef.h"

#include "Resolver.h"
#include "ast/Declaration.h"


AST::Typedef* Resolver::findTypedefInFile(const char* name, AST::File* file)
{
	return nullptr;
}

AST::Typedef* Resolver::findTypedefInModule(const char* name, AST::Module* module)
{
	return nullptr;
}

AST::Typedef* Resolver::findTypedef(const char* name)
{
	return nullptr;
}
