#include "Enum.h"

#include "Resolver.h"
#include "ast/Declaration.h"


AST::Enum* Resolver::findEnumInFile(const char* name, AST::File* file)
{
	return nullptr;
}

AST::Enum* Resolver::findEnumInModule(const char* name, AST::Module* module)
{
	return nullptr;
}

AST::Enum* Resolver::findEnum(const char* name)
{
	return nullptr;
}

AST::EnumValue* Resolver::findEnumValueInFile(const char* name, AST::File* file)
{
	return nullptr;
}

AST::EnumValue* Resolver::findEnumValueInModule(const char* name, AST::Module* module)
{
	return nullptr;
}

AST::EnumValue* Resolver::findEnumValue(const char* name)
{
	return nullptr;
}
