#include "Struct.h"

#include "Resolver.h"
#include "ast/Declaration.h"


AST::Struct* Resolver::findStructInFile(const char* name, AST::File* file)
{
	return nullptr;
}

AST::Struct* Resolver::findStructInModule(const char* name, AST::Module* module)
{
	return nullptr;
}

AST::Struct* Resolver::findStruct(const char* name)
{
	return nullptr;
}
